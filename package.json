{
  "name": "tagteam",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "rm -f main.*.css* src.*.js* && parcel build src/index.html --no-source-maps --out-dir . --public-url ./",
    "start": "parcel src/index.html --out-dir dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "typescript": "^3.5.3"
  },
  "dependencies": {},
  "browserslist": [
    "> 5%"
  ]
}
